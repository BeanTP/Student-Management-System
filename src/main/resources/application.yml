#spring.application.name=student_management_system
server:
  port: 8080
  servlet:
    context-path: /sms

spring:
  datasource:
    url: "jdbc:mysql://localhost:3308/SMSProject"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  security:
    user:
      name: admin
      password: admin123
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false